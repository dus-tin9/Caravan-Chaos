export default function calculateWindowSeatScore(person, seat, allSeats) {
  // Logik: z.B. +10 wenn am Fenster, 0 sonst
  // seat und allSeats stehen zur Verfügung für Nachbarschafts-Logik
  return 10;
}